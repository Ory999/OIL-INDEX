# Secrets
.env
*.env

# Raw PDF and text files (large, regenerated by pipeline)
data/raw/opec_pdfs/
data/raw/eia_steo/
data/raw/aramco_texts/
data/raw/iea_texts/
data/raw/energy_sec/

# Raw data parquet files (large, regenerated daily)
data/raw/*.parquet

# Feature parquet files — ignore large intermediates but keep master datasets
data/features/*.parquet
!data/features/master_quant.parquet
!data/features/master_with_nlp.parquet

# Keep results CSVs and JSONs (small, useful to track in git)
# data/results/ is NOT ignored — pipeline metadata and scores are committed

# Python cache
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# OS files
.DS_Store
Thumbs.db
